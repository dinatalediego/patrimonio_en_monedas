import test from 'node:test';import assert from 'node:assert/strict';import {trustedClaims,parseBcrp} from '../supabase/functions/pm-refresh/auth.js';
const p={sub:'repo:dinatalediego/patrimonio_en_monedas:ref:refs/heads/main',repository:'dinatalediego/patrimonio_en_monedas',repository_id:'1368767373',repository_owner_id:'49383324',ref:'refs/heads/main',workflow_ref:'dinatalediego/patrimonio_en_monedas/.github/workflows/refresh.yml@refs/heads/main',event_name:'schedule',runner_environment:'github-hosted'};
test('only repository main refresh workflow can import',()=>{assert.ok(trustedClaims(p));for(const patch of [{ref:'refs/pull/1/merge'},{repository_id:'123'},{event_name:'pull_request'},{workflow_ref:'other.yml'},{sub:'repo:attacker/repo:ref:refs/heads/main'}])assert.equal(trustedClaims({...p,...patch}),false)});
test('source contract rejects arbitrary data',()=>{assert.throws(()=>parseBcrp({periods:[]},'2026-09-13'));assert.throws(()=>parseBcrp({config:{series:[{name:'Interbancario Venta'}]},periods:[{name:'01.Ene.26',values:['-3']}]},'2026-09-13'))});
